// Header is intentionally minimal — the Board's HUD handles all live stats.
// This file is kept for possible future use but not rendered in App.jsx.
import React from 'react';
const Header = () => null;
export default Header;
